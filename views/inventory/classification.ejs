<!-- content ending -->
<% if (title) { %>
    <h1>
        <%= title %>
    </h1>
    <% } else { res.redirect("/"); } %>

        <!-- message body -->

        <%# message() %>

            <!-- inventory items -->
            <%- grid %>