<%# Edit Review View %>
    <h1>
        <%= title %>
    </h1>

    <%- messages() %>

        <% if (errors) { %>
            <ul class="message warning">
                <% errors.array().forEach(error=> { %>
                    <li>
                        <%= error.msg %>
                    </li>
                    <% }) %>
            </ul>
            <% } %>

                <p>Update your review for the <strong>
                        <%= inv_year %>
                            <%= inv_make %>
                                <%= inv_model %>
                    </strong></p>

                <form id="editReviewForm" action="/review/update" method="post">
                    <fieldset>
                        <legend>Edit Your Review</legend>

                        <label for="review_rating">Rating:</label>
                        <div class="star-rating-input">
                            <% for (let i=5; i>= 1; i--) { %>
                                <input type="radio" id="star<%= i %>" name="review_rating" value="<%= i %>"
                                    <%=review_rating==i ? 'checked' : '' %> required>
                                <label for="star<%= i %>" title="<%= i %> star<%= i > 1 ? 's' : '' %>">&#9733;</label>
                                <% } %>
                        </div>

                        <label for="review_text">Your Review:</label>
                        <textarea id="review_text" name="review_text" rows="6" required
                            minlength="10"><%= review_text %></textarea>

                        <input type="hidden" name="review_id" value="<%= review_id %>">

                        <button type="submit" class="btn-primary">Update Review</button>
                    </fieldset>
                </form>

                <p><a href="/account/">&larr; Back to account management</a></p>